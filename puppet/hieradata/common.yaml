---
classes:
  - profile::base

icinga2::repositoryd: false

nscp::ensure: stopped
nscp::enable: false

puppet::puppetmaster: puppet.icinga-book.local
puppet::environment: production

network::dns: 'no'
